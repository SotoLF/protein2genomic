# Build artifacts
build/
CMakeFiles/
CMakeCache.txt
cmake-build-*/
compile_commands.json
Makefile
*.o
*.a
*.so
*.dylib

# Binaries (in-source / installed). The shell wrapper at bin/prot2exon
# is a checked-in source file and must NOT be ignored.
prot2exon
!bin/prot2exon

# Editor/system
*.log
*.swp
.DS_Store
benchmarks/work/
benchmarks/proteins_list.txt
